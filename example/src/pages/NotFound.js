import React from 'react';
import { FacebookButton, GoogleButton } from 'react-auth';

const ResetPassword = () => {
	return (
		<div>
			<FacebookButton />
			<GoogleButton />
		</div>
	);
}

export default ResetPassword;
